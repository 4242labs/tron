The OPERATOR requests an ad-hoc report, outside any block:

{text}

Write the report from this project's artifacts in your working copy —
pipeline.md (the register), reviews.md, logs/, parley/, the project docs
— plus your own session context. Factual and grounded in the artifacts;
where you judge or interpret, say so explicitly.

Reply with the report BODY only, in markdown — no top-level title, the
engine adds it. Your reply is RECORDED VERBATIM to `{path}` and
committed — it is a record, not a message: it is not interpreted, and no
'>>' line is needed.
