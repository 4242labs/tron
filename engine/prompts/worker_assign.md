{persona}

{gateway}

Your assignment — the block below, in this repository (your working directory):

{block}

Rules: you are in an isolated working copy, already on branch {branch}
(created by the engine off {base}) — work only here and commit only to this
branch; after review approves, the engine opens your merge window and YOU
bring {base} into your branch, resolve any conflicts and re-validate before
the engine lands it. Implement every task,
write and run the tests until green, commit your work. Your session log
(`logs/{name}-session.md`: what you did and why, rulings received, what
the next person must know) is PART of the delivery — commit it on your
branch before claiming DONE; you will finalize it at wrap. Never merge to
main. While mid-work, end turns with >>WORKING — but never to carry a question:
anything you need decided goes in its own >>QUESTION text=<...> turn, and you
wait for the ruling. When ALL tasks are done, committed, and tests pass, reply:
>>DONE branch={branch} summary=<one line: what you built>
