Your >>WRAPPED failed the engine's own verification.

Reason: {reason}

Fix it in this working copy on {branch} (log at its exact path, everything
committed, tree clean), then reply >>WRAPPED branch={branch} summary=<...>
again.
