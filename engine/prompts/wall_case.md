An engine-detected WALL on the block below — the run cannot advance
without a judgment. The engine's attested case:

{case}

The block:

{block}

You rule FIRST — the operator is the last resort, and reaches for you
before anything reaches them. Reply
>>ANSWER text=<guidance for the blocked seat — actionable, one line> if a
ruling can resolve this; reply
>>ESCALATE reason=<why only the operator can resolve it> only if it
genuinely needs the operator (a spec defect, a missing credential, a
decision above your context).
