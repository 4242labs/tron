Your `>>{word}` is not a word this phase accepts — the engine did not act
on it. This phase closes only on {expected}. (>>CONFIRMED is never a phase
word — it exists only as the direct reply to the engine's AC challenge,
which asks for it explicitly.) Continue the phase's work and
reply {expected} when it is true. If you need a decision or information
you cannot get yourself: use >>QUESTION text=<...> if it is in your legal
set; otherwise write plain text with NO '>>' line and the engine routes it.
