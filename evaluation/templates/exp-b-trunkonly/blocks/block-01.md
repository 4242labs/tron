# Block 01 — the scale helper

test: python3 -m unittest discover

## Tasks

1. Create `scale.py` with one function `scale(x)`: return `x * 10`.
2. Unit tests in `test_scale.py`: at least 3 cases, and one of them must
   assert `scale(2) == 20`.
3. The whole repository suite stays green.
