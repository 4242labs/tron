# pipeline.md

The status + sequence ledger for this project. Joins to specs by ID. TRON keeps this current as blocks progress; it is the authoritative running record during a session.

Status values: `todo` · `in-progress` · `blocked` · `review` · `done`.
Order = intended sequence (preference). Spec **dependencies** are the hard gates.

| Order | ID | Owner | Status | Notes |
|:------|:---|:------|:-------|:------|
|       |    |       |        |       |

<!--
Seeded by TRON. Populated at seed time from the operator's answers
(or mapped from an existing host pipeline doc). One row per spec/block.
-->
