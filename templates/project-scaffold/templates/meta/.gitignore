# Host-runtime working files — canon is model-agnostic (agent instructions live in AGENTS.md).
# A host runtime (e.g. an assistant CLI) may drop a CLAUDE.md here; keep it local, never commit it.
CLAUDE.md

# OS cruft
.DS_Store

# TRON's own runtime (ADR-0002 D1, 01-32 T2): manifest, events, cases, grants, worker
# mailboxes, and the scratch dir (per-worker spawn cwd + carved worktrees, plus the
# declared-command trunk-validation checkouts) — TRON's writable folder-absolute
# surface, never project content. Nested scratch worktrees carry their own `.git` FILE
# (git's own convention — the real admin state lives under this repo's `.git/worktrees/`),
# never a real `.git` dir, and must never appear as untracked project content or pollute
# `replica_clean`. Template only here — seat-time population of an actual SIM target
# project is 02-11 T1, same split as land.sh/hook/roles.yaml.
agents/tron/
