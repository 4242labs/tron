{
  "name": "project-lens",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Read-only HTML lens over the project's pipeline / tech-debt / backlog / archive.",
  "scripts": {
    "build": "node build.mjs",
    "serve": "python3 -m http.server 4422 --directory dist"
  },
  "dependencies": {
    "marked": "^14.1.0"
  }
}
