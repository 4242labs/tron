[TRON]  Architect — scope {block} so it's ready to dispatch.

Anything you author goes on a branch — report the branch name (`--tag branch --branch <name>`); I gate it, then you land it on trunk yourself via the grant + `meta/scripts/land.sh` (I never land or merge — that's yours). Report it ready, or say exactly what's blocking the scope.
