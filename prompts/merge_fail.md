Your >>MERGED failed the engine's own verification of the repository:
{reason}
Nothing lands until the trunk really is merged into your branch and the
suite is green on that merged state. Finish the merge on {branch}, commit,
then reply >>MERGED branch={branch} summary=<...> again. If a decision
blocks you, use >>QUESTION text=<...> instead.
