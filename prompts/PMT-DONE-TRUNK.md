[TRON]  {worker_id} — {block}: merged. Now prove it on trunk.

Sync your local to trunk first — the merge may not have been yours to run. Then re-run every applicable acceptance criterion on trunk — the evidence, not a claim. Reply only when each one passes, and open it `done {block} — trunk:` then the evidence.

Anything you commit on your branch after the merge — session logs, doc sync — stays parked there; the close order lands it later. Don't ask me to merge it now.
