[TRON]  {worker_id} — {block}: validate locally.

Go acceptance criterion by acceptance criterion. For each one testable in dev, run it and show me the evidence — the command and its result, not "it passes." Any criterion you can't test locally: name it and say why.

Reply only when every applicable criterion has evidence, and open it `done {block} — local:` then the evidence. A bare "done" doesn't move the gate.
